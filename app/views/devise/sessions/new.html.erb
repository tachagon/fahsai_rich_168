<div class="login-box">

  <div class="login-logo">
    <a href="/"><b>Fahsai Rich </b>168</a>
  </div>

  <div class="login-box-body">

    <h2 class="login-box-msg">เข้าสู่ระบบ</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-group has-feedback">
        <%= f.email_field :login, class: 'form-control', autofocus: true, placeholder: 'อีเมล หรือรหัสสมาชิก' %>
        <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
      </div>

      <div class="form-group has-feedback">
        <%= f.password_field :password, class: 'form-control', autocomplete: "off", placeholder: 'รหัสผ่าน' %>
        <span class="glyphicon glyphicon-lock form-control-feedback"></span>
      </div>

      <div class="row">
        <div class="col-xs-8">
          <% if devise_mapping.rememberable? -%>
            <div class="checkbox icheckbox">
              <label>
                <%= f.check_box :remember_me %> จดจำการเข้าสู่ระบบ
              </label>
            </div>
          <% end -%>
        </div>
        <div class="col-xs-4">
          <%= f.submit "เข้าสู่ระบบ", class: 'btn btn-primary btn-block btn-flat' %>
        </div>
      </div>

    <% end %>

    <div class="login-box-msg">
      <%= render "devise/shared/links" %>
    </div>

  </div>
</div>
