$(document).ready(function () {
	// =========================================
	// new user registration
	// =========================================
	$( "#user_province" ).select2({theme: "bootstrap"});
	// $( "#user_amphur" ).select2({theme: "bootstrap"});
	// $( "#user_district" ).select2({theme: "bootstrap"});
});
